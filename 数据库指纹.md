## 数据库

| 名称 | 版本 | 识别特征 | 验证示例 |
|------|------|---------|----------|
| MySQL | 5.x-8.x | 报错含 `MySQL` 或 `SQLSTATE`，默认端口 3306 | https://www.mysql.com |
| MariaDB | 10.x-11.x | 报错含 `MariaDB`，默认端口 3306 | https://mariadb.org |
| PostgreSQL | 12.x-16.x | 报错含 `PostgreSQL` 或 `PG::Error`，默认端口 5432 | https://www.postgresql.org |
| Oracle Database | 19c-23c | 报错含 `ORA-` 或 `Oracle`，默认端口 1521 | https://www.oracle.com/database |
| SQL Server | 2019-2022 | 报错含 `Microsoft OLE DB Provider for SQL Server`，默认端口 1433 | https://www.microsoft.com/sql-server |
| SQLite | 3.x | 报错含 `SQLite`，文件扩展名 `.db`/`.sqlite` | https://www.sqlite.org |
| Redis | 6.x-7.x | 响应头 `Server: Redis`，默认端口 6379 | https://redis.io |
| MongoDB | 4.x-6.x | 响应头含 `MongoDB`，默认端口 27017 | https://www.mongodb.com |
| Elasticsearch | 7.x-8.x | 响应头 `Server: Elasticsearch`，默认端口 9200 | https://www.elastic.co/elasticsearch |
| OpenSearch | 2.x | 响应头 `Server: OpenSearch`，默认端口 9200 | https://opensearch.org |
| Cassandra | 4.x-5.x | 默认端口 9042，响应含 `Cassandra` | https://cassandra.apache.org |
| ClickHouse | 23.x-24.x | 默认端口 8123/9000，响应含 `ClickHouse` | https://clickhouse.com |
| InfluxDB | 2.x-3.x | 默认端口 8086，响应含 `InfluxDB` | https://www.influxdata.com |
| Neo4j | 4.x-5.x | 默认端口 7474/7687，响应含 `Neo4j` | https://neo4j.com |
| CouchDB | 3.x | 默认端口 5984，响应含 `CouchDB` | https://couchdb.apache.org |
| Memcached | 1.6.x | 默认端口 11211，响应含 `Memcached` | https://memcached.org |
| RabbitMQ | 3.x | 默认端口 15672/5672，响应含 `RabbitMQ` | https://www.rabbitmq.com |
| Kafka | 3.x | 默认端口 9092，响应含 `Kafka` | https://kafka.apache.org |
| Zookeeper | 3.x | 默认端口 2181，响应含 `Zookeeper` | https://zookeeper.apache.org |
